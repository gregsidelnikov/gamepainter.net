<?php include('../Migration/Composition.php');

/*

    $Connection = new db();

    if (!is_numeric($_POST['article_id']))
    {
        print "Article ID is not numeric (" . $_POST['article_id'] . ")";
        return;
    }

    $where = "`key` = '" . $_POST['article_id'] . "'";

    if (db::set("`content`", array("aweber_article_settings_id", "template"), array($_POST['settings_id'], $_POST['template_filename']), $where) != FALSE)
        print "<b>Settings for article #" . $_POST['article_id'] . " are saved!</b>";
    else
        print "<b>Error saving settings.</b>";

*/

?>