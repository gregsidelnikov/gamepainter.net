<?php

/*

    $NewsletterSettings = null;

    function get_aweber_form($idx)
    {
        global $aweber_forms;
        return $aweber_forms[$idx];
    }

    function print_category_name($cat) {
        global $Categories;
        for ($i = 0; $i < count($Categories); $i++) {
            if ($cat == $Categories[$i]["dir"]) {
                print $Categories[$i]["name"];
            }
        }
    }

    function get_category_name($cat) {
        global $Categories;
        for ($i = 0; $i < count($Categories); $i++)
            if ($cat == $Categories[$i]["dir"])
                return $Categories[$i]["name"];
    }
*/

?>